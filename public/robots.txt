# robots.txt for moltbank.bot
#
# Default: allow well-behaved crawlers and agents.
# Utility routes (/logo, /social-image) are not useful to index; they
# exist only to render design assets. They remain fetchable for crawlers
# that want them; they're just excluded from general-purpose indexing.
#
# Machine-readable agent contract: /llms.txt (llmstxt.org spec).

User-agent: *
Allow: /
Disallow: /logo
Disallow: /social-image
